# SMCL - Simple Minecraft Launcher

一个简单的 Minecraft 启动器 (Bash 实现)

**作者**: 像素代码师  
**版本**: 9.4.3

## ✨ 特性

- 🖥️ **跨平台支持**: macOS 和 Linux (仅64位)
- 🔧 **架构支持**: x86_64 / arm64
- 🎮 **Fabric 支持**: 支持 Fabric 模组加载器
- 💾 **离线模式**: 无需 Mojang 账号登录
- 📦 **简洁结构**: 清晰的目录组织

## 📁 目录结构

```
smcl/
├── launcher.sh          # 主启动器 (支持 macOS/Linux, Fabric)
├── simple_launcher.sh   # 简化版启动器 (仅 macOS, 无 Fabric)
├── README.md
└── 1.20.1/              # 游戏版本目录
    ├── 1.20.1.jar       # 游戏主文件 [必须]
    ├── libraries/       # Java 库 [必须]
    │   ├── universal/   # 通用库
    │   └── linux/       # Linux 专有库
    ├── assets/          # 资产目录 [可选]
    │   ├── indexes/     # 资产索引
    │   └── objects/     # 资产对象
    ├── lwjgl-natives-<系统>-<架构>/  # LWJGL natives [必须]
    ├── natives-<系统>-<架构>/        # 运行时 natives (自动生成)
    └── run/             # 游戏运行目录
        ├── saves/       # 存档
        ├── resourcepacks/  # 资源包
        ├── options.txt  # 游戏设置
        ├── logs/        # 日志
        └── crash-reports/  # 崩溃报告
```

## � 首次使用

**重要**: `assets.zip` 文件由于体积较大未包含在仓库中，请手动下载并解压：

1. 从 [Release 页面](https://github.com/zhtxiii-ops/smcl/releases) 下载 `assets.zip`
2. 解压到 `1.20.1/` 目录：
   ```bash
   cd 1.20.1
   unzip assets.zip
   ```

## �🚀 快速开始

### 1. 配置启动器

编辑 `launcher.sh` 中的配置项:

```bash
java_path='java'           # Java 路径
is_fabric="false"          # 是否使用 Fabric
mc_version="1.20.1"        # Minecraft 版本
username="test"            # 用户名
uuid="123e4567e89b..."     # 32位 UUID (不含 -)
memory=4096                # 内存 (MB)
```

### 2. 运行游戏

```bash
# 完整版启动器 (macOS/Linux)
./launcher.sh

# 简化版启动器 (仅 macOS)
./simple_launcher.sh
```

## 📋 版本对比

| 特性 | launcher.sh | simple_launcher.sh |
|------|------------|-------------------|
| macOS 支持 | ✅ | ✅ |
| Linux 支持 | ✅ | ❌ |
| Fabric 支持 | ✅ | ❌ |
| 资产支持 | ✅ | ❌ |

## ⚠️ 注意事项

1. **libraries 目录**: 不要放入无关的 jar 文件, 确保所有依赖完整且版本正确
2. **游戏文件**: 确保 `$mc_version.jar` 存在且完整
3. **如果使用 Fabric**: 需将 Fabric Loader 相关库放入 libraries 目录
4. **模组加载器**: 仅支持 Fabric, 不支持 Forge 等其他加载器

## 🔧 支持的平台

| 操作系统 | 架构 | 状态 |
|---------|------|------|
| macOS | x86_64 | ✅ |
| macOS | arm64 (Apple Silicon) | ✅ |
| Linux | x86_64 | ✅ |
| Linux | arm64 | ✅ |
| Windows | - | ❌ |

## 📝 License

MIT License
